#!/usr/bin/env bash

rm -rf CMakeLists.txt
rm -rf cmake-build-debug
make clean

basedir=`pwd`
echo $basedir

rm -rf $basedir/logs
mkdir $basedir/logs

auto/configure --with-pcre=/usr/local/include --prefix=$basedir --with-debug
